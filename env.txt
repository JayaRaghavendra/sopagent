# Copy to .env and fill values

# Base directory for relative paths (defaults to script directory)
# BASE_DIR=c:\\Users\\e430107\\Downloads\\SOP_Agent

# Paths (relative to BASE_DIR or absolute)
CONTACTS_FILE=contacts.xlsx
CHECKS_FILE=checks.xlsx
REPORT_FILE=report.xlsx

# Provider auto-selection rules:
# - If LLMFOUNDRY_TOKEN is set, Azure OpenAI will be used automatically and OPENAI_API_KEY is ignored.
# - Else if OPENAI_API_KEY is set, standard OpenAI is used.
# - Else heuristic routing is used (no LLM call).

# Azure OpenAI settings
base_url=
LLMFOUNDRY_TOKEN= 
AZURE_API_VERSION=2025-01-01-preview
OPENAI_MODEL=gpt-4o-mini   # Azure deployment name

# Standard OpenAI settings (ignored when LLMFOUNDRY_TOKEN is set)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Optional: external .env path
# ENV_FILE=c:\\path\\to\\your\\.env


